pipeline {

    agent { dockerfile true }
    stages {
       stage('Test') {
            
            steps {
                sh 'pytest -q'
            }
        }
         stage('Lint') {
           
            steps {
                sh 'pip install -r requirements.txt flake8 mypy'
                sh 'make lint'
            }
        }
    }
}
